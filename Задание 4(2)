#include <iostream>
using namespace std;
int main()
{
	char s[256];
	cout << "Введите строку: ";
	cin.getline(s, sizeof(s));
	for (char *p = s; *p; p++)
	{
		(*p)++;
	}
		cout << "Сдвигаем строку \n ";
	cout << "Результат: " << s << "\n";
	return 0;
}
